# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'GlobalVibes' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!

  # Pods for GlobalVibes
  pod 'Google-Mobile-Ads-SDK', '~> 7.28'
  pod 'Firebase/Core'
  pod 'Firebase/AdMob'
  pod 'Firebase/Core'
  pod 'Fabric', '~> 1.7.2'
  pod 'Crashlytics', '~> 3.9.3'
  pod 'Firebase/Performance'
  
  target 'GlobalVibesTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'GlobalVibesUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
